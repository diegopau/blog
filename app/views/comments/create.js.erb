/* Insert a notice between the last comment and the comment form */
$("#comment-notice").html('<div class="flash notice"><%= escape_javascript(flash.delete(:notice)) %></div>');
//TODO: el letrerito de "thanks for commenting" debería estar solo un tiempo.

/* Replace the count of comments */
$("#comments_count").html("<%= pluralize(@comment.post.comments.count, 'Comment') %>");
//TODO: hay un bug en el contador de comentarios

/* Add the new comment to the bottom of the comments list */
$("#comments").append("<%= escape_javascript("#{render :partial => 'posts/comment', :locals => { :comment => @comment }}").html_safe %>"); //la funcion escape_javascript sirve para mandarle un monton de codigo y que lo inserte sin intentar interpretarlo, en este caso se le envia
                                                                                                                                                   // todo el codigo correspondiente a un comentario

/* Highlight the new comment */
$("#comment_<%= @comment.id %>").effect("highlight", {}, 3000);

/* Reset the comment form */
 $("#comment_form")[0].reset();