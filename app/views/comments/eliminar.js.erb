/* Eliminate the comment by fading it out */
$('#comment_<%= @comment.id %>').fadeOut(); //para identificar correctamente un comentario, al estar mezclados los comentarios de varios post, será necesario identificarlo tanto por su id como la id de su post.
/* Replace the count of comments */
/* TODO: hay un problema con la cuenta que se hace al eliminar comentarios */
$("#comments_count").html("<%= pluralize(Comment.count, 'Comentario') %>");