<p>Tags:</p>
<ul>
  <% @tags.each do |tag| %>

      <% if tag.name == @tag %>
          <% puts "passssssssssssoooooooooo" %>
         <li><%= link_to "#{tag.name} (#{tag.posts.count})", {:controller => 'posts', :action => 'search_result', :tag => tag.name}, :class => "selected_tag" %> </li>
      <% else %>
         <li><%= link_to "#{tag.name} (#{tag.posts.count})", {:controller => 'posts', :action => 'search_result', :tag => tag.name} %> </li>
      <% end %>
  <% end %>
</ul>